server:
  prot: 8761
spring:
  profiles: dev
  application:
    name: eureka-client

eureka:
  instance:
    hostname: eureka8761
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
---
server:
  port: 8761
spring:
  profiles: test
  application:
    name: eureka-client
    
eureka:
  instance: 
    hostname: eureka8761
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false